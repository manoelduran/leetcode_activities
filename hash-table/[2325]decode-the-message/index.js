/**
 *
 * @param {string} key
 * @param {string} message
 * @return {string}
 */
var decodeMessage = function (key, message) {};

const key = "12345";
const message = "abcde";
const result = decodeMessage(key, message);
console.log(result);
